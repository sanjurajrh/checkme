apiVersion: v1
kind: Service
metadata:
  labels:
    app: hello
  name: hello
  namespace: project1
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: hello
  type: ClusterIP
